#include <stdio.h>
#include <stdio.h>

/**
* main - print if the number is positive, zero, or negative
*
* Return:: Always zero
*
*/
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%i", n);
		n++;
	}
	printf("\n");

	return (0);
}
